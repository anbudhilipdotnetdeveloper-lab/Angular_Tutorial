import { MouseOverColorChange } from './mouse-over-color-change';

describe('MouseOverColorChange', () => {
  it('should create an instance', () => {
    //const directive = new MouseOverColorChange();
  //  expect(directive).toBeTruthy();
  });
});
