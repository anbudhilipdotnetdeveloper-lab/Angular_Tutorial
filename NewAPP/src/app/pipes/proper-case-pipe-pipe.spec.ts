import { ProperCasePipePipe } from './proper-case-pipe-pipe';

describe('ProperCasePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ProperCasePipePipe();
    expect(pipe).toBeTruthy();
  });
});
